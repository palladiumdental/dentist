export type TPriceType = {
  id: string;
  order: string;
  enService: string;
  huService: string;
  price: string;
  priceTo?: string;
  onPromotion: boolean;
  promotionPrice: string;
  promotionPriceTo: string;
  serviceCategory: string;
};
